
## 下午记录（14:30）

### neirong 不响应问题
- 根因：升级到 4.8 后 gateway 重启，导致 session 积压，上下文爆了
- neirong 自述：上下文溢出 1000 多条，15K token 限制
- 解决：neirong 自己触发了一次压缩，session 从大变小
- cron "isolated" 机制无效——isolated cron 无法真正压缩主 session
- **结论**：需要 neirong 自己收到消息才能触发自动压缩，isolated cron 是假动作

### 架构调整（上午讨论）
- linggangshenghuo / liaoyuyewu 是 main 的子脑，不需要独立飞书通道
- 飞书里只有 main 有独立 bot 账号，linggangshenghuo 和 liaoyuyewu 走 main 内部调度
- 已给 main 配备 multi-agent-coordinator skill 和 SKILL_DISPATCH.md

### session flush 机制
- neirong 有每4小时 cron（session-context-flush-neirong），但 isolated 模式无效
- 真正压缩靠：neirong 收到消息后自动触发，或者手动发消息给它
- 已写脚本：~/.openclaw/scripts/session-flush-if-needed.py（但未注册成 cron）

### 升级到 4.8
- 升级后 gateway 重启多次，cron 任务状态错乱
- 重启要用 `openclaw gateway restart`，不要用 stop+start

## 歌曲生成流程（记住！）

施欢确认：以后写歌 = MiniMax music API。今天做的《深海的锚》效果很好。

**标准流程**：
1. 读取飞书文档歌词
2. MiniMax API：`https://api.minimaxi.com/v1/music_generation`，model=`music-2.5`
3. **关键**：返回的 `audio` 字段是 **hex 字符串**，不是 base64！用 `bytes.fromhex()` 解码
4. 保存到桌面，文件名格式：`系统+歌曲名.mp3`（标注"系统"区分施欢自己做的版本）
5. 验证 MP3 文件头（`49443304` = ID3）

**API Key**：`sk-api-XRtmMXcMJOVrhtYCoAMNoyL_EoyjXFxCcKut7b-ePtweO8hrOXi8IONJCVUm6640dhgvFJPqp2aBVPeUCnAO9k0zmEUK3sHsKgERfPCGYWTCGvK1HoMlyY8`

**注意**：不能用 clawhub 下载的 music skill（解码方式错了，是 base64），要直接用 hex 解码。
